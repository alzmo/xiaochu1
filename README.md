# xiaochu1

项目已从**微信小程序页面结构**迁移为**微信小游戏结构**，用于后续按小游戏路线继续开发和提审。

## 当前版本说明
- 入口改为 `game.js` + `game.json`。
- 渲染改为小游戏 Canvas 2D 方案（不依赖 WXML/WXSS 页面体系）。
- 保留核心玩法：
  - 分层方块 + 顶层可点击；
  - 5 个暂存槽；
  - 默认 2 个收纳框、每个容量 3；
  - 满 3 清空并刷新颜色；
  - 胜利/失败判断；
  - 自动关卡生成。

## 项目当前状态
### 1. 已完成功能
- 小程序已迁移为微信小游戏结构，使用 `game.js` + `game.json` 启动。
- 核心玩法已可运行：分层遮挡点击、暂存槽、收纳框自动消除、胜负判断。
- 已实现自动关卡生成，并在开局阶段做可解性校验（内置求解器）。
- 已支持局内提示一步、增加暂存槽/收纳篮（能力入口已提供）。
- 已支持关卡进度本地存档与下一关预准备。

### 2. 当前正在开发内容
- 广告帮助能力目前为“预留接口”状态，尚未接入真实广告流程。
- 关卡体验仍在持续调优（难度曲线、无解局面提示与搜索开销平衡）。
- 渲染层以功能可用为主，动画与音效仍在后续迭代范围。

### 3. 下一步计划
- 接入真实广告能力与发放逻辑，完善帮助功能闭环。
- 补充动画、音效和反馈细节，提升操作手感与可读性。
- 继续优化关卡生成与求解性能，降低复杂局面的等待感。
- 增加更多可配置玩法参数，支持后续提审与长期运营迭代。

## 在微信开发者工具中运行
1. 打开微信开发者工具，选择“导入项目”。
2. 项目目录选择当前仓库根目录。
3. 项目类型选择“小游戏（Game）”。
4. 使用测试号（如 `touristappid`）即可本地调试。
5. 编译后会从 `game.js` 启动。

## 目录结构（精简后）
- `game.js`：小游戏入口。
- `game.json`：小游戏配置。
- `src/levelGenerator.js`：关卡与方块布局自动生成。
- `src/gameState.js`：遮挡判断、点击处理、暂存槽与收纳框逻辑、胜负判断。
- `src/renderer.js`：Canvas 2D 渲染与点击区域布局。

